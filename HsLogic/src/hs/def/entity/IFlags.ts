"use strict";

namespace Def {

    export interface IFlags {
        immune?: boolean,
        elusive?: boolean,
        divine_shield?: boolean,
        charge?: boolean,
        forgetful?: boolean,
        freezed?: boolean,
        stealth?: boolean,
        taunt?: boolean,
        windfury?: boolean,
        silenced?: boolean
    }
}